# 项目协作说明

- 先读根目录 `README.md`，再读 `Docs/PROJECT.md`、`Docs/WORKFLOW.md` 和 `plan/README.md`。执行任务时只补读本轮说明与任务引用的专题。
- 制作人负责方案、任务拆分、目录与 review；程序、美术、音频执行者按当前用户指派认领任务。不要自行把后续版本变成当前工作。
- 文档默认 Markdown，简洁、按主题拆分。任务交付直接填写原任务文件，不另生成多份重复报告。
- `plan/` 只留当前未完成或待 review 工作；完成后由制作人移入 `archive/` 并维护链接。历史设计不覆盖当前任务。
- 美术和音频交到各自 `SourceAssets/`；程序执行者负责 Unity 工程与资源集成，避免多人同时改同一场景或项目设置。
- 可运行的工作要实际运行并报告结果；未测项目如实标注，不用前置检查代替执行或测量。
- 默认不新增 hash，不冻结 contract、baseline 或 gate；不删除已有安全措施。门禁仅用于不可逆、跨系统、安全或正式发布边界。
- Unity 工程首次建成并运行后，询问用户是否建立 GitHub 仓库并提交；未得到指示前不创建远端或推送。

当次用户给出的分工与指令优先。具体参数与交付要求查当前任务，不在此复制数值。
